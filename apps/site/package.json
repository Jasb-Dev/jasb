{
  "name": "@jasb/site",
  "version": "0.1.0",
  "private": true,
  "description": "jasb.dev \u2014 the landing page, the legal pages, and the door to the demo.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "echo \"no TypeScript here: the site is static HTML and CSS\"",
    "test": "echo \"no unit tests: the site is static HTML and CSS\""
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@jasb/ui": "workspace:*"
  }
}
