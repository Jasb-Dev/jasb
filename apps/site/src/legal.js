/**
 * The legal pages need the stylesheet and nothing else.
 *
 * A separate entry from the landing page so the demo's fetch logic is not
 * shipped to a reader who only wanted the privacy policy.
 */
import "./site.css";
